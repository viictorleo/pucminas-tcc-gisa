<%= form_with(model: health_wallet) do |form| %>
  <% if health_wallet.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(health_wallet.errors.count, "error") %> prohibited this health_wallet from being saved:</h2>

      <ul>
        <% health_wallet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
