<% if notice.present? %>
<div class="bg-gray-100 rounded-lg py-6 mb-8 mx-8 flex items-center justify-center">
  <p style="color: black"><%= notice %> </p>
</div>
<% end %>

<div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-8 px-8">
  <% if policy(:authorization_requests).index? %>
    <div class="bg-slate-300 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Informações Cadastrais</p></div>
  <% end %>

  <%= link_to associate_services_path do %>
    <div class="bg-slate-300 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Serviço ao Associado</p></div>
  <% end %>
    <% if policy(:authorization_requests).index? %>

  <div class="bg-slate-300 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Gestão e Estratégia</p></div>
    <% end %>

    <% if policy(:authorization_requests).index? %>

  <div class="bg-slate-300 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Ciência de Dados</p></div>
    <% end %>

    <% if policy(:authorization_requests).index? %>

  <div class="bg-slate-200 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Relatório Financeiro</p></div>
    <% end %>

    <% if policy(:authorization_requests).index? %>

  <div class="bg-slate-200 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Reenviar Boleto</p></div>
    <% end %>

    <% if policy(:authorization_requests).index? %>

  <div class="bg-slate-200 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Teleatendimento</p></div>
    <% end %>

    <% if policy(:authorization_requests).index? %>

  <%= link_to authorization_requests_path do %>
    <div class="bg-slate-200 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Autorização</p></div>
  <% end %>
      <% end %>


  <%= link_to settings_path do %>
    <div class="bg-slate-200 rounded-lg py-8 h-30 sm:h-40 flex items-center justify-center"><p>Configurações</p></div>
  <% end %>
</div>